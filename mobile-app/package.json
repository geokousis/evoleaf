{
  "name": "evoleaf-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Android wrapper for EvoLeaf using Capacitor.",
  "scripts": {
    "android:add": "cap add android",
    "android:open": "cap open android",
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
